# 贡献指南

NovaAgent 采用小步、可验证的提交方式。提交代码前请确认变更属于 PRD
中的明确任务，并在必要时更新对应 ADR 或追踪链接。

## 开发环境

- 支持 Python 3.12 和 3.13；建议使用隔离虚拟环境和项目锁定的依赖。
- 从仓库根目录运行 `make check`。该命令是合并前的最小门禁，必须在本地
  通过后再提交。
- Core 代码必须能够在无网络、数据库和可选依赖的环境中导入和测试。

## 工作流

1. 从最新的 `dev` 创建短生命周期分支，并将一个分支限定为一个可审查目标。
2. 先写或更新验收测试/fixture，再实现最小变更；跨包边界变更先补 ADR。
3. 每个独立的小功能点单独提交。提交正文说明行为、边界、不变量、测试
   命令和是否存在迁移影响；不要把格式化或无关重构混进功能提交。
4. 每个功能提交通过 `make check` 后立即推送到远程分支，便于恢复、审查和
   追踪；推送失败时在后续操作前记录原因并重试。
5. Pull Request 描述关联的 PRD/ADR 条目、测试证据、风险和未完成事项。

## 提交信息

使用简短的 Conventional Commit 标题，例如
`feat(core): add frozen turn identity contract`。正文应至少回答：

- 解决了哪个验收条目；
- 关键设计和不变量是什么；
- 执行了哪些检查及其结果；
- 是否改变公共 schema、版本或迁移要求。

## 依赖与边界

依赖只能沿架构图向下流动：适配器 → runtime → react/graph → core。Core
不得导入具体存储、网络、CLI、MCP 或 UI 实现。新增依赖必须说明用途、许可、
锁定方式和为何不能由标准库或已有端口承担。

## 安全与数据

不要在 issue、日志、trace、cassette 或测试 fixture 中提交 API key、真实
个人数据或未脱敏的敏感参数。工具副作用必须通过 `ToolExecutor` 和
`PolicyGateway`；危险能力默认拒绝。

